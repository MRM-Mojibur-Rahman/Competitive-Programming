#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int note , page;
    cin>>note>>page;
    long long ans = note*page*100;
    cout<<ans<<endl;
    return 0;
}
