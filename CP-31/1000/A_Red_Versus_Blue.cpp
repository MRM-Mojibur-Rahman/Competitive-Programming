#include <bits/stdc++.h>
using namespace std;
#define ll long long

void solve()
{
    int n, r, b;
    cin >> n >> r >> b;
    int sz = r / (b + 1);
    int l = 0;
    while (l < n)
    {
        
    }
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;
    while (t--)
    {
        solve();
    }
    return 0;
}
