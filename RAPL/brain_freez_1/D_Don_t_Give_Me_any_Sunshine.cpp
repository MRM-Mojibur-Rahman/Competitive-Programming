#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n, m;
    cin >> n >> m;
    vector<int> cordi(n);
    vector<int> um(m);
    for (int i = 0; i < n; i++)
    {
        cin >> cordi[i];
    }
    for (int i = 0; i < m; i++)
    {
        cin>>um[i];
    }
    int j = 0;
    int coverd = INT_MIN;
    int ansr = 0 ;
    for (int i = 0; i < n; i++)
    {
        
    }
    
    
  

    return 0;
}
