#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;
    cin >> n;
    deque<int> v(n);
    for (int &x : v)
        cin >> x;
    int serja = 0, dima = 0;
    bool s = true, d = false;
    while (!v.empty())
    {
        if(s)
    }
    
   

    cout << serja << " " << dima << endl;

    return 0;
}
